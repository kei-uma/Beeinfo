<%= form_for(twitter_datum) do |f| %>
  <% if twitter_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twitter_datum.errors.count, "error") %> prohibited this twitter_datum from being saved:</h2>

      <ul>
      <% twitter_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
